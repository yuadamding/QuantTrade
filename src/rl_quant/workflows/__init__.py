"""Workflows layer: versioned run presets / specs that compose datasets + models + training + evaluation into
named, reproducible experiments. Part of the protocol-first layered architecture
(see architecture_migration_plan.md). Submodule: ``presets``. The old ``rl_quant.presets`` import path
remains working via a shim."""
